# -*- coding: utf-8 -*-
# vim: ft=yaml
---
promtail:
  pkg:
    name: promtail
  rootgroup: root
  config: '/etc/promtail'
  service:
    name: promtail
  subcomponent:
    config: '/etc/promtail-subcomponent-formula.conf'
  # Just here for testing
  added_in_defaults: defaults_value
  winner: defaults
